name: Save Hot Post every 7 minutes

on:
  schedule:
    - cron: "*/7 * * * *"
  workflow_dispatch:

jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call save-hot-post API
        run: |
          curl -X GET https://cron-api-server.vercel.app/api/save-hot-post
